I need to start adding tools

make a command for a chat window to replace current buffer

tool call output visibility appears to break the markdown formatting. Should fix that eventually

I want to make it so that hitting enter in normal mode is actually what submits

I want to make it so that the tool call small version still shows the args and some kind of hint about the output. then I will make it so that the tool call output command goes away, and you can scroll the tool calls and select them with space and it shows it to you in a popup.

I should consider separating the history out of the agent loop to make it more extensible.

Add session support, this will mean integration with sqlite and a bunch more commands
